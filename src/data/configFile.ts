interface Props {
  motorSpeedMin: number;
  motorSpeedMax: number;
  temperatureMin: number;
  temperatureMax: number;
}

export const configData: Props = {
  motorSpeedMin: 0,
  motorSpeedMax: 200,
  temperatureMin: 0,
  temperatureMax: 110,
};
